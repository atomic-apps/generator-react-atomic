// styled-components
import styled from 'styled-components';

// <%= name %>
const <%= name %> = styled.<%= styledComponentTag %>`
    /* Enter your css code inside of here */
`


export default <%= name %>;
